version: '3'
services:
  site:
    build: .
    command: bundle exec jekyll serve --livereload --host 0.0.0.0
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - .:/app
    environment:
      - JEKYLL_ENV=development 